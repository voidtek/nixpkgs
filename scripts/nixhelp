#!/usr/bin/env bash

echo "🛠️  Nix Development Environments"
echo ""
echo "📦 Available Profiles:"
echo "  nix-default   - Basic dev tools (git, curl, htop, shellcheck)"
echo "  nix-python    - Python development (python3, pip, uv, pylint)"
echo "  nix-docker    - Containerization (docker, docker-compose, buildx)"
echo "  nix-hugo      - Static sites (hugo, nodejs, go)"
echo "  nix-devops    - Infrastructure (terraform, kubectl, helm, aws-cli)"
echo "  nix-ai        - AI/ML development (python3, jupyter, numpy, pandas)"
echo ""
echo "🚀 Direct Commands:"
echo "  nix develop .#profile    - Enter environment locally"
echo "  nix develop github:voidtek/nixpkgs#profile - Remote"
